name: 'idho_varen'
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.2

dependencies:
  animations: ^3.0.0
  csv: ^8.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/worker_data.csv
fonts:
  - family: Mukta-Malar
    fonts:
      - fonts/MuktaMalar-Regular.ttf